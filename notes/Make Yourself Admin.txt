Make Yourself Admin
Run this in terminal inside backend:

node -e "const mongoose = require('mongoose'); const dotenv = require('dotenv'); dotenv.config(); mongoose.connect(process.env.MONGO_URI).then(async () => { const User = require('./models/User'); await User.updateOne({ email: 'sanvith@gmail.com' }, { isAdmin: true }); console.log('✅ Admin set!'); process.exit(); });"